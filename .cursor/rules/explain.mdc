---
description: 
globs: 
alwaysApply: true
---
我需要把深圳市划分成20 * 20 的栅格地图，其中一些栅格会包含气象站点，我需要利用这些气象站点的臭氧浓度数据进行反距离权值插值，对每一个地图栅格点进行插值（被站点覆盖的栅格就使用实际数据值）。

除了这个栅格化地图对应的臭氧浓度数据，我的输入还有4个分别朝向东南西北的、视野面积覆盖了这栅格化地区的摄像头在不同时间戳所拍摄到的图像。

最后，我的项目目标是制造一个 conv-lstm 模型，用卷积神经网络处理摄像头图像，加上插值得到的栅格化臭氧浓度数据数组，作为模型的输入；
我希望这个模型最后可以根据一系列照片的输入，预测出下一时间戳的臭氧浓度数据

[idw.py](mdc:src/interpolation/idw.py) 是用于对栅格化地图进行差值的代码
[generate_JOSN_index.py](mdc:src/utils/generate_JOSN_index.py) 是用于生成 JSON 格式数据的代码
[transforms.py](mdc:src/utils/transforms.py) 是进行数据增强的代码（不一定有用）